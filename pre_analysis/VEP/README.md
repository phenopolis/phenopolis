# Importing data from VEP into db

VEP produces JSON output which we then postprocess for import into the db.

The annotations we include are:
* ExAC freq
* Kaviar freq
* CADD (TODO: computed for every variant)
